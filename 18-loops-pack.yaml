- name: install packages
  hosts: frontend
  become: yes
  tasks:
  - name: install packages using loop
    ansible.builtin.package:
      name: "{{ item.name }}"
      state: "{{item.state}}"
    loop:
      - name: nginx
        state: present
      - name: mysql
        state: present
      - name: zip
        state: present
